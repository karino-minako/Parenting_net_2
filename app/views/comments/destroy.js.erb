$(".post_<%= @post.id %>").html("<%= j( render 'posts/show', post: @post ) %>");
$(".comment_<%= @post.id %>").html("<%= j( render 'posts/comments', comments: @post.comments.order(created_at: :desc).page(params[:page]) ) %>");