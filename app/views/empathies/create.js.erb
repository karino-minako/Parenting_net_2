$(".question_<%= @question.id %>").html("<%= j( render 'questions/show', question: @question ) %>")

$(".questions_<%= @question.id %>").html("<%= j( render 'questions/question', question: @question ) %>")